// Сохраните эти правила и примените их в консоли Firebase для вашей базы данных Firestore
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Временно разрешаем полный доступ для работы приложения
      allow read, write: if true;
      
      // В будущем можно использовать более ограниченные правила, например:
      // allow read, write: if request.auth != null;
    }
  }
}
